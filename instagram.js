const axios = require('axios');
const config = require('./config.json');

const fetchFollowers = (id, instagramId, accessToken) => {

};

const fetchFollowings = (id, instagramId, accessToken) => {

};

const fetchProfile  = (instagramId, accessToken) => {

};

const generateSignature = (endpoint, params) => {

};

const fetchStats = (accessToken) => {

};

module.exports = {
  fetchFollowers,
  fetchFollowings,
  fetchProfile,
  fetchStats,
};
